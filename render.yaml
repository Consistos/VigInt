services:
  # API Server - runs api_proxy.py with integrated video storage
  - type: web
    name: vigint-api-server
    env: python
    region: oregon
    plan: starter  # Can upgrade to standard or pro
    buildCommand: pip install -r requirements-api-server.txt
    startCommand: python api_proxy.py
    healthCheckPath: /api/health
    disk:
      name: video-storage
      mountPath: /opt/render/project/src/uploads
      sizeGB: 10  # Adjust based on your needs
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: HOST
        value: 0.0.0.0
      - key: SECRET_KEY
        generateValue: true
      - key: SPARSE_AI_API_KEY
        sync: false  # Optional: Set same as SECRET_KEY or different
      - key: GOOGLE_API_KEY
        sync: false  # Must be set manually
      - key: DATABASE_URL
        fromDatabase:
          name: vigint-db
          property: connectionString
      - key: VIDEO_STORAGE_DIR
        value: /opt/render/project/src/uploads
      - key: VIGINT_CONFIG_PATH
        value: /opt/render/project/src/server_config.ini
      - key: FLASK_ENV
        value: production
      - key: RENDER
        value: "true"
      - key: EMAIL_SMTP_SERVER
        sync: false
      - key: EMAIL_SMTP_PORT
        value: 587
      - key: EMAIL_USERNAME
        sync: false
      - key: EMAIL_PASSWORD
        sync: false
      - key: EMAIL_FROM
        sync: false
      - key: EMAIL_TO
        sync: false

databases:
  # PostgreSQL database for API server
  - name: vigint-db
    databaseName: vigint
    user: vigint
    plan: plus  # New paid plan - no expiration, includes backups
    region: oregon
